---
# AUTO-GENERERT av scripts/generate-inventory.py
# Ikke rediger direkte — endre config/lab.yml og kjor scriptet pa nytt.

all:
  children:
    windows:
      vars:
        ansible_connection: winrm
        ansible_winrm_transport: basic
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5985
        ansible_user: Administrator
        ansible_password: Lab123
      children:
        domain_controllers:
          hosts:
            dc01: &id001
              ansible_host: 10.50.0.10
        member_servers:
          hosts:
            fs01: &id002
              ansible_host: 10.50.0.20
        dhcp_servers:
          hosts:
            dc01: *id001
        file_servers:
          hosts:
            fs01: *id002
